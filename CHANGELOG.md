# Changelog

本项目遵循 [Semantic Versioning](https://semver.org/)。

## Unreleased

### Added

- 终端支持保留内容的 DEC 软复位，以及清空内容和状态的硬复位。
- 终端支持为会话或新会话默认值配置 Enter 的 CR/LF/CRLF 与 Backspace 的 DEL/BS 映射。
- 会话管理器支持通过原生文件对话框导入/导出版本化 JSON，并安全处理重复会话 ID。
- 发送窗格支持导入/导出文本与 Hex 命令模板，导入时追加并重建重复 ID。
- 增加最多 500 条的本地结构化诊断记录、敏感上下文脱敏、导出和清空功能。
- YModem 支持批量文件接收、CRC/块号校验、双 EOT、进度、取消和原生目录保存。
- 增加基于 Apache-2.0 zmodem.js 的 ZModem 批量收发、`rz`/`sz` 检测、进度、取消和安全落盘。
- 增加隔离进程运行的 xterm.js 10 万、50 万、100 万行解析、回滚、搜索和内存基线工具。
- 增加带事务、WAL、白名单键和 schema version 迁移的本地 SQLite 配置存储。
- 原生应用启动时把既有会话、偏好、工作区和发送器数据迁入 SQLite，并保持有序同步。
- 增加类型安全的中英文翻译框架、持久化语言偏好，并完成应用设置界面双语化。
- 顶层菜单、会话工具栏、欢迎页、状态栏、错误条和快捷键帮助支持中英文切换。
- 会话管理器的搜索、配置操作、设备分组和数量状态支持中英文切换。
- 发送窗格、命令模板提示和 Y/ZModem 文件传输状态支持中英文切换。
- 终端搜索、Hex 空状态、复位工具和连接加载状态支持中英文切换。
- 会话编辑器的基础信息、串口、SSH、ADB、工具检测和连接校验支持中英文切换。
- 会话编辑器的终端、窗口、日志、触发器及触发器校验支持中英文切换。
- 新建、复制会话的默认名称与未配置连接目标提示会跟随界面语言。
- 工作区恢复、重连、触发器、日志、文件传输、导入导出和确认提示支持中英文切换。
- 增加协议与后端错误本地化层，英文界面可翻译常见串口、文件、日志及 X/Y/ZModem 错误。
- 文件接收原生目录选择器与诊断导出的隐私说明、脱敏标记支持中英文。
- 终端启用 Unicode 11 字符宽度规则，改善 CJK、Emoji 与宽字符对齐。
- 顶部“搜索”菜单和搜索图标可直接打开当前活动终端的查找框。
- 标签列表按钮提供可访问的会话下拉菜单，支持当前项标识、切换和外部关闭。
- 终端支持复制选区、粘贴和全选命令，并提供 ⌘C/⌘V 与 Ctrl+Shift+C/V 快捷键。
- 顶部十组菜单接入会话、编辑、查找、导航、布局、模式、日志、串口工具、主题和帮助操作。

### Fixed

- 切换界面语言后，已恢复工作区的连接提示会立即刷新为当前语言。

## 0.4.0 - 2026-07-27

### Added

- SSH 支持不保存凭据的密码和键盘交互认证。
- 会话设置可检测 OpenSSH 与 Android Platform Tools，并在缺失时给出安装提示。
- 应用和终端支持浅色、深色与跟随系统主题，并记住用户选择。
- 增加新建、关闭、切换标签、连接、侧栏、发送窗格、专注模式等核心快捷键及帮助面板。
- 增加应用设置层：可配置新会话默认协议、终端、日志及活动会话关闭确认，当前会话配置优先。
- 会话支持文本/正则触发器，可跨接收块匹配并执行通知、发送文本或开始日志动作。
- 支持左右/上下双终端分屏、标签替换当前窗格和分屏工作区恢复。
- 文件发送新增 XModem-CRC，支持 C 握手、CRC16、ACK/NAK 重试、EOT 和双 CAN 取消。
- 文件传输新增 YModem 批量发送，包含文件名/大小块 0 元数据和批次结束空块。
- SSH 与 ADB 会话会随终端窗格变化同步远程 PTY 行列数。
- SSH 与 ADB 会话支持原始字节/文本日志及开始、暂停、继续、停止和自动记录。
- 所有会话支持按单文件大小限制自动轮转，并可配置保留份数。
- 工具栏可安全打开当前日志文件或应用日志目录。
- SSH/ADB 子进程改用跨平台本地伪终端，改善交互式 Shell 行为。

## 0.3.0 - 2026-07-27

### Added

- Serial、SSH、ADB 三类会话统一配置、保存和工作区恢复；
- OpenSSH Agent/默认密钥和指定私钥连接，支持主机密钥校验及保活；
- ADB 设备发现、授权状态和设备元数据展示；
- 交互式 SSH 与 ADB Shell 终端、发送、断开和进程错误提示。

## 0.2.0 - 2026-07-26

### Added

- Text/Hex 接收双视图、行时间戳和 8/16/24/32 列 Hex 分组；
- 终端查找、清空接收视图和实时行列状态；
- 原始字节与可读文本日志，支持开始、暂停、继续、停止和自动记录；
- 原始文件分块发送、进度显示、写入背压和取消；
- A/B/C/D 同步输入通道与专注模式；
- 会话复制、删除、活动会话关闭确认；
- 串口热插拔刷新、USB 稳定身份匹配与有上限的自动重连；
- 串口输入/输出硬件缓冲清理；
- 工作区、打开标签和每会话发送器模板恢复。

### Fixed

- 重连前清理失效的串口会话句柄；
- 没有活动会话时不再错误显示日志故障状态；
- 终端重新创建时从受控接收缓冲恢复内容。

## 0.1.0 - 2026-07-26

首个可运行预览版本。

### Added

- Tauri 2、Rust、React 与 xterm.js 桌面应用；
- WindTerm 风格的串口工作区、会话树与多标签终端；
- 本机串口枚举、连接、收发、DTR、RTS 与 Break；
- 文本/Hex、行尾、多发送器与循环发送；
- 会话持久化、错误提示和 RX/TX 统计；
- Windows、macOS、Linux CI 与 Release 自动化。
